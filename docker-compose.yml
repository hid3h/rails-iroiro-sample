version: '3'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    # tty: true
    volumes:
      - .:/myapp
    env_file:
      - .env
    ports:
      - "3003:3000"
